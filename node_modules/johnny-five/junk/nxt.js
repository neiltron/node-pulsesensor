var Board = require("firmata").Board,
    address, board;

// NXT MMX
address = 0x06;

board = new Board("/dev/tty.usbmodem411", function() {

  console.log( "Ready" );



  board.on("string",function( string ) {
    console.log("string", string);
  });

  // // Continuous measurement mode
  // board.sendI2CWriteRequest( 0x41,
  //   [ 0xb0, 0x5A, 0x55 ]
  // );
  //
  //
  //

  // command, location, bytes
  // getDeviceID 0x10, 8
  // voltage 0x41

  board.sendI2CConfig();

  board.sendI2CWriteRequest( address,
    [ 0x01, new Buffer([90, 5000, 0, 0x01]) ]
  );

  // board.sendI2CWriteRequest( address,
  //   [ 0x52, 0 ]
  // );

//0x41
  // Read from register
  // board.sendI2CReadRequest( address, 4, function( data ) {


  //   console.log( "received", data.length, data );


  // });

});
