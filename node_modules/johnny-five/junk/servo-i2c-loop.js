var five = require("../lib/johnny-five.js"),
    board, servos, count, degrees, interval;

board = new five.Board();

board.on("ready", function() {
  var min, max;

  min = 150;
  max = 600;

  servos = {
    i2c: new five.Servo({ i2c: true, pin: 0 })
  };

  for ( var pulselen = min; pulselen < max; pulselen++ ) {
    console.log( pulselen );
    servos.i2c.setPWM( 0, pulselen );
  }

  // five.Fn.sleep(500);

  // for ( var pulselen = max; pulselen > min; pulselen-- ) {
  //   servos.i2c.setPWM( 0, pulselen );
  // }

  // five.Fn.sleep(500);


});
